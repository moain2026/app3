/**
 * React Native config — العباسي تحصيل
 *
 * Used by `npx react-native-asset` to link fonts and other assets.
 */

module.exports = {
  project: {
    android: {},
  },
  assets: ['./assets/fonts/'],
};
