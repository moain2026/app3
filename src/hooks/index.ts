/**
 * Hooks — barrel.
 *
 * Custom React hooks shared across screens. Import via `@/hooks` (the
 * `@/` alias resolves to `src/`).
 */

export { usePrinter, type UsePrinterResult } from './usePrinter';
